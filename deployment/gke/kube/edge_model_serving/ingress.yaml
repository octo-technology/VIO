apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
#    ingress.kubernetes.io/ingress.allow-http: "false"
  name: edge-model-serving
  namespace: vio
spec:
  defaultBackend:
    service:
      name: edge-model-serving
      port:
        number: 8080
#  rules:
#  - host: inference.vio.octo.tools
#    http:
#      paths:
#      - backend:
#          service:
#            name: edge-model-serving
#            port:
#              number: 8080
#        path: /*
